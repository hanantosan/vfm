{
  "name": "Flutter Dev Container",
  "image": "mcr.microsoft.com/devcontainers/dotnet:0-6.0",
  "postCreateCommand": "git clone https://github.com/flutter/flutter.git -b stable && echo 'export PATH=\"$PATH:/workspaces/${{containerWorkspaceFolderBasename}}/flutter/bin\"' >> ~/.bashrc && source ~/.bashrc && flutter doctor",
  "customizations": {
    "vscode": {
      "extensions": [
        "Dart-Code.dart-code",
        "Dart-Code.flutter"
      ]
    }
  }
}
